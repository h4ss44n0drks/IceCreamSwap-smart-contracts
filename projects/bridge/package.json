{
  "name": "bridge",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.ts"
  },
  "dependencies": {
      "common": "*"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^4.5.0"
  }
}
